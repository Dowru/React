import React, { useState } from 'react';
import "../mensaje.css"

const Header = ({ text }) => {

    return (
        <header>
            <nav>
                <h1>REACT</h1>
            </nav>
        </header>
    );
}
export default Header;